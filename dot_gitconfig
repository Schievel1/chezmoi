[user]
	name = Pascal Jäger
	email = pascal.jaeger@leimstift.de
[init]
	defaultBranch = main
[safe]
	directory = %(prefix)///wsl$/Gentoo/home/pascal/dev/docker_trdp_rohling
	directory = %(prefix)///wsl$/Gentoo/home/pascal/dev/docker_trdp_rohling
	directory = /var/tmp/portage/app-editors/neovide-0.12.2/work
[alias]
	c   = checkout
	cb  = checkout -b
	l   = log --all --decorate  --graph --oneline
	log = log --graph
	s   = status --short

# [core]
    # pager = less

# [merge]
    # conflictstyle = diff3

[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true  # use n and N to move between diff sections
    dark = true      # or light = true, or omit for auto-detection
    line-numbers = true
    side-by-side = true
    diff-so-fancy = true

[merge]
    conflictstyle = zdiff3

[diff]
    colorMoved = default
[github]
	user = Schievel1
[credential]
	helper = cache
